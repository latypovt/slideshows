title: Slideshows
url: "https://latypovt.github.io"   # no trailing slash
baseurl: "/slideshows"               # project site => MUST start with '/'

# Use Just the Docs (provides all the includes your layout calls)
remote_theme: just-the-docs/just-the-docs

# Required for remote_theme on GitHub Pages
plugins:
  - jekyll-remote-theme

# (optional – JtD options)
just_the_docs:
  search_enabled: true
