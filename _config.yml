title: Slideshows
url: "https://latypovt.github.io"     # no trailing slash
baseurl: "/slideshows"                 # MUST start with a slash
theme: jekyll-theme-minimal            # gives you a default layout
plugins: []                            # avoid unsupported plugins for now

# If you want to keep using `_pages` as a collection:
collections:
  pages:
    output: true
    permalink: /:path/
